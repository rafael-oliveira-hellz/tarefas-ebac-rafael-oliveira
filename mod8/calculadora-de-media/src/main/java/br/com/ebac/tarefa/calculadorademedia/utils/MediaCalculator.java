package br.com.ebac.tarefa.calculadorademedia.utils;

public class MediaCalculator {
    public double calculate(double grade1, double grade2, double grade3, double grade4) {
        return (grade1 + grade2 + grade3 + grade4) / 4;
    }
}
